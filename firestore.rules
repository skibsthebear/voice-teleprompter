rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow authenticated users to read and write all transcripts
    // Since there's only one user (you), this provides simple but secure access
    match /transcripts/{transcriptId} {
      allow read, write: if request.auth != null;
    }
  }
}
